








/*

let updatedProfile = UserProfile.map((user) => {
  Transaction.forEach((transaction) => {
    if (user.email === (transaction.to || transaction.from)) {
      user.transactions.push(transaction)
    }
  })
  return user
})


*/
